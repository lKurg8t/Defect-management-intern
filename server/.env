PORT=4000
NODE_ENV=development

# MySQL (XAMPP)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=defect_db

# Auth
JWT_SECRET=change_me_in_production

# Optional (only if you want to seed via /api/auth/seed-admin)
ALLOW_SEED=false
